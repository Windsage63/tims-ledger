# AGENTS.md

## Important Note

NEVER install any package into the global Python environment. This repo is designed to be used with the installed .venv virtual environment.

Always begin a terminal session by checking to be sure that you are in the .venv virtual environment. If you are not, then run the command `source .venv/bin/activate` to activate it.
